# Normalize all text files to LF regardless of the contributor's OS or git
# config. Without this, Windows checkouts with core.autocrlf=true store CRLF
# in the working tree, which Prettier (endOfLine defaults to "lf") always
# flags — that's what was failing the CI lint step.
* text=auto eol=lf
